---
- name: Start ombt server(s)
  docker_container:
    image: "{{ ombt_version }}"
    command: "{{ item.command }}"
    name: "{{ item.name }}"
    detach: "{{ item.detach }}"
    network_mode: host
    state: started
  with_items: "{{ ombt_confs['rpc-server'][inventory_hostname] }}"
