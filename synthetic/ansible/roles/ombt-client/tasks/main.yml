---
- name: Install and start docker image "ombt" in client mode
  shell: >
         docker run -d -ti beyondtheclouds/ombt
         --debug --control {{ broker }}://{{ hostvars[groups['amqp-control'][0]]['ansible_' + control_network]['ipv4']['address'] }}
         --url {{ broker }}://{{ hostvars[groups['amqp-queue'][0]]['ansible_' + control_network]['ipv4']['address'] }} rpc-client


